package com.fillexc.repository;

import com.fillexc.domain.SocialName;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SocialNameRepository extends JpaRepository<SocialName, Integer> {
}
